import React from "react";
import DoWRow from "./DoWRow";

const Calendar = () => {
    return (
        <table>
            <thead>
            <DoWRow />
            </thead>
        </table>
    );
};

export default Calendar;